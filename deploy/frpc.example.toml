# Windows 电脑端 frpc 配置示例
# 映射 4725 到 VPS；外网通过 nginx + HTTPS 域名 churuku.duckdns.org 访问

serverAddr = "154.83.91.109"
serverPort = 7000

auth.method = "token"
auth.token = "请替换为与服务器一致的 token"

[[proxies]]
name = "jade-inventory-web"
type = "tcp"
localIP = "127.0.0.1"
localPort = 4725
remotePort = 4725
