---
description: 珠宝吊牌：编号与数字条码规则
globs: agents/print-agent/**,apps/web/src/**/Label*.tsx,apps/web/src/lib/labelPrintSync.ts,apps/web/src/lib/labelPrintMemory.ts
alwaysApply: false
---

# 编号 vs 条形码（固定规则）

- **编号 `certNo`**：镯子业务编号，吊牌文字「编号:xxx」用此字段，扫码枪查库存也用编号/条码字段匹配 DB。
- **条形码内容**（与编号不同）：数字条码，公式固定为  
  **`前缀 + round(成本×3+10) + floor(圈口)`**  
  例：前缀 02、成本 1000、圈口 57.0 → `02301057`  
  实现见 `apps/web/src/lib/labelPrintSync.ts` 的 `computeBarcodeDigits`。
- 前缀来自批次前两位或用户设置的条码前缀（`labelPrintMemory.barcodePrefix`）。
- 打印时条码行编码上述数字串；PQAPI/PNG 路径以模板 `barcode` 行的 `format` 为准。
- 禁止把编号字符串直接当作条码内容（除非数字条码恰好与编号相同）。

# 偏移

- 模板字段：`offsetTopMm` / `offsetBottomMm` / `offsetLeftMm` / `offsetRightMm`（毫米）。
- 上、左正值向下/右移；下、右正值向上/左移。净位移：`dx = left - right`，`dy = top - bottom`。
